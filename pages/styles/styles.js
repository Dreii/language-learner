import { StyleSheet } from 'react-native'

export const inputStyles = StyleSheet.create({
    button: {
        borderWidth: 0.5,
        borderColor: '#000000',
        marginBottom: '6px'
    },
    text: {
        borderWidth: 0.5,
        borderColor: '#000000',
        marginBottom: '6px'
    }
})
